services:
  lofi-studio:
    build:
      context: .
      args:
        - VITE_GEMINI_API_KEY=${GEMINI_API_KEY}
    ports:
      - "8080:80"
    restart: always
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
